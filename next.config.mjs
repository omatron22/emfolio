/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: [],
    unoptimized: false,
  },
  reactStrictMode: true,
};

export default nextConfig;